# Services Content

MDX/JSON source files for each service, matching the shape the Services feature expects. Structure these to mirror the eventual Sanity schema so the migration is a data-source swap, not a rewrite.
