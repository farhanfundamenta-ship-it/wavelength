# Contact Feature

Feature module. Owns everything specific to this one business domain: page-section composition, data-shaping hooks, local types, and constants. Cross-feature-reusable UI goes in components/ instead — only put something here if moving it out would require no changes anywhere else.
