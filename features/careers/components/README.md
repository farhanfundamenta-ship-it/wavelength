# Careers — Components

Feature-scoped UI: sections and layouts unique to the careers pages, built by composing components/ primitives. Not reused by any other feature — the moment something here is needed elsewhere, promote it to components/.
