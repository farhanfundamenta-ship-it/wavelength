# Provider Composition

Composes the app's context providers into a single tree (ThemeProvider, LenisProvider, MotionConfigProvider) consumed once from the root layout. Distinct from context/: context/ defines each raw context; this folder wires them together.
