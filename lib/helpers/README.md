# Domain-Adjacent Helper Functions

Pure functions that support the app but aren't generic enough for utils/ and aren't tied to one specific library (e.g. buildBreadcrumbs, resolveRouteTitle). If a helper has zero app-specific knowledge, it belongs in utils/ instead.
