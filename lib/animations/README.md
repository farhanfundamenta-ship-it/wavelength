# Shared Animation Definitions

GSAP timelines and Framer Motion variant objects reused across multiple features (page-transition variants, scroll-triggered timelines). Concrete configuration, not components — components/animations consumes this.
