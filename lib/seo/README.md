# SEO Helpers

generateMetadata() builders, JSON-LD structured-data builders, canonical-URL resolvers, OG image helpers. Every route's metadata export should go through here rather than hand-rolling metadata objects per page.
