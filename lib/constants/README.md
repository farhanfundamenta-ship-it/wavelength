# Library-Level Constants

Constants tied to the lib/ integration layer specifically: breakpoint values consumed by hooks, animation durations/easing tokens, z-index scale used by motion/provider code. App-wide non-technical constants belong in the root constants/ folder.
