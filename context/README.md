# React Context Providers

Raw React.createContext + Provider definitions, one concern per file (theme-context, locale-context, navigation-context). Distinction from lib/providers: this folder defines each context in isolation; lib/providers composes all of them into the single tree mounted in the root layout.
