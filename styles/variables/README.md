# Design Tokens

CSS custom properties: color scale, spacing scale, radii, shadow scale, z-index scale. The canonical source for values that both Tailwind config and hand-written CSS should reference — never hardcode a hex value outside this folder.
