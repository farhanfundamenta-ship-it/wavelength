# Framework-Agnostic Utilities

Pure, dependency-free helper functions: string/number/array/object manipulation (formatCurrency, truncate, capitalize, groupBy, slugify). Zero imports from React, Next.js, or any UI library — must be safely importable from any layer (components, features, lib, services) without risking a circular dependency. Anything that needs a library import belongs in lib/utils instead.
