# Global Reusable Hooks

Generic browser/UI hooks with no external library dependency: useMediaQuery, useOutsideClick, useDebounce, useLockBodyScroll, useIsMounted. Distinction from lib/hooks: anything here could be copy-pasted into an unrelated project with zero changes. Hooks that wrap a named dependency (Lenis, GSAP) belong in lib/hooks instead.
