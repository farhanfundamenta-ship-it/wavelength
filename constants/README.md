# Application Constants

App-wide fixed values with zero library dependency: route path strings, nav breakpoints (non-CSS), pagination page sizes, external URLs. Distinction from config/: constants/ holds values a developer changes; config/ holds values closer to content (nav structure, SEO defaults) that a non-engineer might eventually edit via CMS.
