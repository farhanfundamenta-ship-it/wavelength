# Cross-Cutting Composites

Small composites used by two or more features that don't cleanly fit any folder above: Logo, ThemeToggle, LanguageSwitcher, ScrollProgress. Last resort, not a dumping ground — if a component only serves one feature, it belongs in that feature's components/ folder instead.
