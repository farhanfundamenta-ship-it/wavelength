# Form Primitives

Controlled input primitives: Input, Textarea, Select, Checkbox, Radio, FormField, FormError. Validation logic and submit handlers stay in the owning feature (e.g. features/contact); this folder only renders and reports state.
