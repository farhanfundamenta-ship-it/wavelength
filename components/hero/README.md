# Hero Section Variants

Reusable hero layouts (HeroDefault, HeroSplit, HeroVideo, HeroMinimal) used across marketing pages. Content (headline, media) is passed in as props — heroes never fetch their own data.
