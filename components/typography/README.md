# Typography Components

Heading, Text, Eyebrow, RichText renderer (for CMS-sourced rich text once Sanity is connected). Enforces the type scale instead of ad-hoc Tailwind text classes scattered through the app.
