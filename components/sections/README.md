# Generic Page Sections

Composable, content-agnostic section blocks used to build pages: CTASection, StatsSection, LogoCloud, FeatureGrid, TestimonialSection. If a section is reused across 2+ features, it belongs here, not inside a single feature folder.
