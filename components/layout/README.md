# Layout Primitives

Structural chrome shared across every route: Header, Footer shell, PageWrapper, Container, Section, Grid. These define page skeleton, not page content.

Rule: a component belongs here only if it has no opinion about what page it's on.
