# Theme Config

Design tokens exposed to JS/TS (color map, font map, breakpoint map) — the JS-side mirror of styles/variables, consumed by tailwind.config and any JS that needs a token value at runtime.
