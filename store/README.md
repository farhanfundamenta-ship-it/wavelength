# Global State Store

Reserved for client-side global state once local component state and Context stop being sufficient (e.g. Zustand). Empty until a real cross-cutting state need appears — do not add a store just because the folder exists.
