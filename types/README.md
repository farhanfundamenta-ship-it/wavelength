# Global TypeScript Types

Types shared across the entire app: global.d.ts augmentations, API response shapes, CMS document shapes once Sanity is integrated. Feature-specific types stay in features/*/types — only promote a type here once two or more features need it.
