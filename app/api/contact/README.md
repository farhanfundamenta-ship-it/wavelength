# Contact Form Endpoint

POST handler for the /contact page form. Validates input, sends email / forwards to CRM. Keep validation schemas in the contact feature (features/contact/types or a schema file there), not inline here.
